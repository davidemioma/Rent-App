"use client";

import React from "react";
import { FilteredProperty } from "@/types";

type Props = {
  property: FilteredProperty;
  showFavoriteBtn: boolean;
};

const CardCompact = ({}: Props) => {
  return <div>CardCompact</div>;
};

export default CardCompact;
